import { StyleSheet, Text, View } from "react-native";
import React from "react";

const RenderText = () => {
  return (
    <View testID="renderText">
      <Text>RenderText</Text>
    </View>
  );
};

export default RenderText;

const styles = StyleSheet.create({});
